services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tg-discord-bridge
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SQLITE_PATH: /data/bot.db
    volumes:
      - ./data:/data
